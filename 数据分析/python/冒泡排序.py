# 
def bubble(arr):
	n=len(arr)
	for i in range(n):
		j in range(1,n-i):
			if arr[j-1] < arr[j]:
				
		
		
		
def insertSort(arr):
	n = len(arr)
	for i in range(1,n):
		cont = arr[i]
		j = i-1
		while j>=0:
			if arr[j]>cont
				arr[j+1] = arr[j]
				arr[j] = cont
			j-=1
	return arr	