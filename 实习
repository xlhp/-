@date 2019-8-15
jquery : eval()方法处理字符串， 当你面对两个字符串比大小的时候，eval方法可以帮你处理
        attr(name,value)方法可以获取属性值，也可以设置属性值
        html()方法可以设置该对象下的标签啦，属性啦， 反正就是啥都能写进去，解析器也会对该方法设置的标签进行解析
        text（）方法，可以设置内容，该方法设置的内容不会被编译器进行解析
        val（）方法，获取对象内的值，只能获取值。
        append（）方法，可以将其他属性衔接到该对象后面。
java ： Calendar 类可以用来获取日期的前几天或者某几天
mysql : UNIX_TIMESTEMP（）方法可以将日期转换成int类型，精确到秒数

软件开发过程中常用环境：
        dev : development environment 开发环境，用于开发者调试使用
        fat：feature acceptance test environment 功能验收测试环境，用于测试进行测试
        uat：user acceptance test environment 用户验收测试环境，用于生产环境下软件测试的测试使用
        pro：Production environment 生产环境

